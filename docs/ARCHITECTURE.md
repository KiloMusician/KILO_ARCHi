# One companion, existing owners

CompanionStore coordinates the native SwiftUI/AppKit app's assistance requests, confirmed settings, shared document copy, local lessons, placement and selected artwork. KIN's Core Seed and body presentation are one continuing individual. Presentation choices do not create another identity or permission owner.

Qwen, optional Codex and deliberate Compare retain separate lane requests and receipts. Native admission checks, captured source/context references and cancellation fences govern what can become the current answer. Explicitly kept lessons and local temporary records retain their existing correction, withdrawal and expiry rules; private local context is excluded from the external lane.

Work together operates on a deliberately shared text copy. Its object-of-interest flow starts with KIN Seed pointing and a mint window outline. Hover selects Window Server metadata only. Explicit Read captures local Accessibility text, or performs local OCR on an image of the chosen window when required and permitted. DesktopInterestSession binds the bounded read to that target, rejects stale/late results and places successful text into Review. Use in Work together adopts the snapshot through the existing source owner; it does not continuously synchronize the external app.

Selection, proposals and Apply bind to the current working-copy revision. The external app and imported original stay unchanged; Undo and export operate on the local copy. Local Qwen is the default for captured text, and a separate exact-copy allowance gates Codex/Compare. Reading, reviewing or adopting does not invoke inference, keep a lesson or award development. The image is used only for OCR, is not persisted/sent and adds no image-semantics or camera capability. Placement and staff cues remain existing native presentation state.

Node Lab derives nodes and links from current native source metadata, kept lessons, temporary records and lane receipts. Provider/request scope keeps repeated reference labels distinct. Historical metadata cannot restore withdrawn lesson text. The graph is an inspection/navigation projection with bounded input and output; it creates no persistence, model calls, agent swarm or inferred relationship database.

CompanionEvolution, saved preferences/lessons and the retained game's Journey have distinct existing retention boundaries. Recovery controls do not make temporary replies or the shared working copy durable. Inspect and export wanted work before restore or quit experiments.

The current native desktop build does not start the retained WebKit Habitat/Arena. The Journey and battle reducers, Relay and the native/TypeScript bridge remain in source with their tests. They are not replaced by a second authoritative game save in Unity.

The [Unity source project](../unity/ARCHi/README.md) uses bundled KIN artwork, session-only equipment/cues and a pure C# Relay practice domain. The runtime is explicitly disconnected from saved KIN, does not own assistant clients, does not write native memories, and uses a stationary Seed reference badge. A future native/Unity handoff needs a defined contract through these existing owners.

Blender authoring originals, local model installations, private research and user profiles are outside this distribution. Optional Reactor has a local artwork fallback and its own setup. The arc directory remains an isolated synthetic exact-grid evidence lab, not a live ARC competition solver.

Creator provenance, editions, verified ownership and marketplace operations are separate future services. They cannot derive authority from a copied local UID. The [Hampton Designed policy](HAMPTON_DESIGNED.md) preserves MIT permission and excludes private assistant data from transferable records.


## Native local items

Marketplace Discover/My items/Create reuses CompanionEquipment and CompanionPresenceArt. Strict bounded CompanionItemPackage recipes have computed content identity; exact bundled catalog matches expose local registration only. NativePreferenceDocument v5 retains a maximum eight recipes through the existing atomic/conflict/recovery path. Equip is session-only until explicit Save preferences. Removal reconciles the library and saved outfit in the same admission. User-kept gesture settings take precedence over creator defaults. Decorative designs cannot point.

All current canonical-effect lists are empty. Unregistered recipes have zero canon battle/progression influence, and the paused Arena does not consume them as gameplay inputs. Production registry signatures, ownership, scarce editions, payments and named-ruleset loadout validation remain future work. See [local marketplace](LOCAL_MARKETPLACE.md).
